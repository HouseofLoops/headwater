# Headwater API - Development Dependencies
# Install with: pip install -r requirements-dev.txt
# Last updated: 2026-09-23

# Include production dependencies
-r requirements.txt

# Testing
pytest==9.1.1
pytest-cov==7.1.0
pytest-asyncio==1.4.0

# Code Quality
# ruff is the only linter/formatter (config in pyproject.toml). flake8, black and
# isort were listed here but never run by CI or pre-commit; they were removed.
# Keep this pin in step with .github/workflows/_verify.yml and .pre-commit-config.yaml.
ruff==0.16.8

# Type Checking
mypy==2.3.1

# Security
pip-audit==2.10.1

# Development Tools
# httpie was removed: 3.2.4 is the latest release and still carries
# PYSEC-2023-242 with no fix available. Use curl/httpx instead.
watchfiles==1.3.0

